extends base
block title
    {{ site.title }}
block content
    entries = get_data(type='post', limit=300).group("parent_path")
    for c, posts in entries
        h2= c
        ul
            for post in posts
                li
                	a(href=post.url)= post.title
